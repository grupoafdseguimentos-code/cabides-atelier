// Cabides Atelier — Atelier Editorial: boutique digital com branco osso, madeira, metal e verde pinho.
import { useEffect, useMemo, useState } from "react";
import { ArrowRight, Check, Menu, Minus, Plus, Search, ShoppingBag, Sparkles, X } from "lucide-react";
import { Link } from "wouter";
import { categories, formatBRL, products, type Product } from "@/data/catalog";

type CartItem = Omit<Product, "hardware" | "personalization"> & { quantity: number; hardware?: string; personalization?: string };
const STORAGE_KEY = "cabides-atelier-cart";
const ASAAS_CHECKOUT_URL = import.meta.env.VITE_ASAAS_CHECKOUT_URL as string | undefined;

function ProductCard({ product, onOpen }: { product: Product; onOpen: (p: Product) => void }) {
  return <article className="product-card" onClick={() => onOpen(product)}><div className="product-image-wrap"><span className="product-index">{product.featured ? "Destaque" : product.category}</span><img src={product.image} alt={product.name} loading="lazy" /><button className="quick-view" type="button" onClick={(e) => { e.stopPropagation(); onOpen(product); }}>Ver detalhes <ArrowRight size={15} /></button></div><div className="product-meta"><p className="eyebrow">{product.category}</p><h3>{product.name}</h3><div className="product-bottom"><strong>{formatBRL(product.price)}</strong><span>à vista</span></div></div></article>;
}

function ProductDrawer({ product, onClose, onAdd }: { product: Product | null; onClose: () => void; onAdd: (item: CartItem) => void }) {
  const [hardware, setHardware] = useState(product?.hardware?.[0] || product?.colors?.[0] || "");
  const [personalization, setPersonalization] = useState("Nenhuma");
  const [quantity, setQuantity] = useState(1);
  useEffect(() => { setHardware(product?.hardware?.[0] || product?.colors?.[0] || ""); setPersonalization("Nenhuma"); setQuantity(1); }, [product]);
  if (!product) return null;
  const fee = personalization === "Laser — frente" ? 2 : personalization === "Laser — frente e verso" ? 4 : personalization === "Silk — frente" ? 1.5 : personalization === "Silk — frente e verso" ? 3 : 0;
  return <div className="drawer-backdrop" onClick={onClose}><aside className="product-drawer" onClick={(e) => e.stopPropagation()}><button className="icon-button drawer-close" onClick={onClose} aria-label="Fechar detalhes"><X size={20} /></button><div className="drawer-image"><img src={product.image} alt={product.name} /></div><div className="drawer-content"><p className="eyebrow">{product.category}</p><h2>{product.name}</h2><p className="drawer-description">{product.description}</p><div className="drawer-price">{formatBRL(product.price + fee)} <small>por unidade</small></div>{(product.hardware || product.colors) && <label className="field-label">{product.hardware ? "Escolha a ferragem" : "Acabamento"}<select value={hardware} onChange={(e) => setHardware(e.target.value)}>{(product.hardware || product.colors || []).map((o) => <option key={o}>{o}</option>)}</select></label>}{product.sizes && <label className="field-label">Tamanho<select><option>{product.sizes[0]}</option>{product.sizes.slice(1).map((o) => <option key={o}>{o}</option>)}</select></label>}{product.personalization && <label className="field-label">Personalize seu cabide<select value={personalization} onChange={(e) => setPersonalization(e.target.value)}><option>Nenhuma</option><option>Laser — frente</option><option>Laser — frente e verso</option><option>Silk — frente</option><option>Silk — frente e verso</option></select></label>}{product.personalization && <p className="helper-note"><Check size={14} /> A arte final será validada antes da produção.</p>}<div className="quantity-row"><span>Quantidade</span><div className="quantity-control"><button onClick={() => setQuantity(Math.max(1, quantity - 1))} aria-label="Diminuir"><Minus size={15} /></button><strong>{quantity}</strong><button onClick={() => setQuantity(quantity + 1)} aria-label="Aumentar"><Plus size={15} /></button></div></div><button className="primary-button full" onClick={() => { onAdd({ ...product, quantity, hardware, personalization }); onClose(); }}>Adicionar ao carrinho <ShoppingBag size={17} /></button><button className="text-button full" onClick={() => ASAAS_CHECKOUT_URL ? window.location.assign(ASAAS_CHECKOUT_URL) : onClose()}>Comprar pelo Asaas <ArrowRight size={16} /></button></div></aside></div>;
}

export default function Home() {
  const [activeCategory, setActiveCategory] = useState("Todos");
  const [query, setQuery] = useState("");
  const [cart, setCart] = useState<CartItem[]>(() => { try { return JSON.parse(localStorage.getItem(STORAGE_KEY) || "[]"); } catch { return []; } });
  const [selectedProduct, setSelectedProduct] = useState<Product | null>(null);
  const [cartOpen, setCartOpen] = useState(false);
  const [menuOpen, setMenuOpen] = useState(false);
  useEffect(() => localStorage.setItem(STORAGE_KEY, JSON.stringify(cart)), [cart]);
  const filtered = useMemo(() => products.filter((p) => (activeCategory === "Todos" || p.category === activeCategory) && `${p.name} ${p.category} ${p.tags.join(" ")}`.toLowerCase().includes(query.toLowerCase())), [activeCategory, query]);
  const total = cart.reduce((sum, item) => sum + item.price * item.quantity, 0);
  const addToCart = (item: CartItem) => setCart((current) => [...current, item]);
  const removeFromCart = (index: number) => setCart((current) => current.filter((_, i) => i !== index));
  const whatsappText = encodeURIComponent(`Olá! Quero conhecer o Cabides Atelier. ${cart.length ? `Tenho interesse em ${cart.map((i) => `${i.name} (${i.quantity} un.)`).join(", ")}.` : "Gostaria de conhecer o catálogo."}`);
  return <div className="atelier-site"><header className="site-header"><Link href="#inicio" className="brand"><img src="/cabides-atelier-logo.jpg" alt="Cabides Atelier" /><span>Cabides<br /><em>Atelier</em></span></Link><nav className={menuOpen ? "nav open" : "nav"}>{[["Catálogo", "catalogo"], ["Personalização", "personalizacao"], ["Atelier", "atelier"], ["Contato", "contato"]].map(([label, id]) => <a key={id} href={`#${id}`} onClick={() => setMenuOpen(false)}>{label}</a>)}</nav><div className="header-actions"><button className="search-trigger" onClick={() => document.getElementById("catalog-search")?.focus()} aria-label="Buscar"><Search size={18} /></button><button className="bag-button" onClick={() => setCartOpen(true)} aria-label={`Carrinho com ${cart.length} itens`}><ShoppingBag size={19} /><span>{cart.length}</span></button><button className="menu-button" onClick={() => setMenuOpen(!menuOpen)} aria-label="Abrir menu"><Menu size={20} /></button></div></header><main><section className="hero" id="inicio"><div className="hero-copy"><p className="eyebrow">ATELIER DE EXPOSIÇÃO · 2026</p><h1>O primeiro olhar começa <em>no detalhe.</em></h1><p className="hero-lede">Cabides personalizados e soluções de exposição para lojas, boutiques e marcas que cuidam da apresentação como parte da experiência.</p><div className="hero-actions"><a className="primary-button" href="#catalogo">Ver catálogo <ArrowRight size={17} /></a><a className="text-button" href="#personalizacao">Personalizar meu cabide <ArrowRight size={16} /></a></div><div className="hero-note"><span>01</span><p>Materiais escolhidos<br />para sua coleção</p></div></div><div className="hero-image"><img src="/products/catalogo-003.jpg" alt="Cabide personalizado em madeira" /><div className="image-caption"><span>PX—01</span><span>Madeira · Metal · Precisão</span></div></div></section><section className="manifesto" id="atelier"><div className="section-index">01 / ATELIER</div><div><p className="eyebrow">Para quem apresenta com intenção</p><h2>O suporte certo muda<br /><em>a leitura da coleção.</em></h2></div><p className="manifesto-text">Do cabide de madeira ao expositor de provador, o Atelier reúne soluções documentadas, combináveis e prontas para acompanhar a rotina de uma marca de moda.</p></section><section className="catalog-section" id="catalogo"><div className="section-heading"><div><p className="eyebrow">02 / CATÁLOGO</p><h2>Escolha por <em>material e função.</em></h2></div><div className="search-box"><Search size={16} /><input id="catalog-search" value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Buscar no catálogo" /></div></div><div className="category-strip">{categories.map((category) => <button key={category} className={activeCategory === category ? "active" : ""} onClick={() => setActiveCategory(category)}>{category}</button>)}</div><div className="products-grid">{filtered.map((product) => <ProductCard key={product.id} product={product} onOpen={setSelectedProduct} />)}</div>{filtered.length === 0 && <div className="empty-state"><Sparkles size={22} /><p>Nenhum produto encontrado com este filtro.</p></div>}</section><section className="custom-section" id="personalizacao"><div className="custom-image"><img src="/products/catalogo-019.jpg" alt="Detalhe de acabamento e personalização" /></div><div className="custom-copy"><p className="eyebrow">03 / PERSONALIZAÇÃO</p><h2>Seu nome, sua marca,<br /><em>seu acabamento.</em></h2><p>Escolha entre gravação a laser sem pigmentação ou personalização Silk em uma cor. O adicional é calculado por lado do cabide e a arte final é validada antes da produção.</p><div className="custom-options"><div><strong>Laser</strong><span>R$ 2,00 por lado</span></div><div><strong>Silk</strong><span>R$ 1,50 por lado + tela de R$ 150,00</span></div></div><a className="primary-button" href="#catalogo">Escolher um cabide <ArrowRight size={17} /></a></div></section><section className="service-band" id="contato"><div><p className="eyebrow">04 / ATENDIMENTO</p><h2>Para pedidos especiais,<br /><em>converse com o Atelier.</em></h2></div><div className="service-text"><p>Precisa de volume, uma combinação específica ou ajuda para escolher ferragens? Nossa equipe pode orientar o próximo passo.</p><a className="dark-button" href={`https://wa.me/5500000000000?text=${whatsappText}`} target="_blank" rel="noreferrer">Falar pelo WhatsApp <ArrowRight size={17} /></a></div></section></main><footer className="site-footer"><div className="footer-brand"><img src="/cabides-atelier-logo.jpg" alt="Cabides Atelier" /><div><strong>Cabides Atelier</strong><span>Materiais para apresentar melhor.</span></div></div><div className="footer-links"><a href="#catalogo">Catálogo</a><a href="#personalizacao">Personalização</a><a href="#contato">Contato</a></div><span className="footer-copy">© 2026 Cabides Atelier</span></footer><ProductDrawer product={selectedProduct} onClose={() => setSelectedProduct(null)} onAdd={addToCart} />{cartOpen && <div className="drawer-backdrop" onClick={() => setCartOpen(false)}><aside className="cart-drawer" onClick={(e) => e.stopPropagation()}><div className="cart-heading"><div><p className="eyebrow">SEU PEDIDO</p><h2>Carrinho <em>({cart.length})</em></h2></div><button className="icon-button" onClick={() => setCartOpen(false)} aria-label="Fechar carrinho"><X size={20} /></button></div>{cart.length === 0 ? <div className="empty-cart"><ShoppingBag size={26} /><p>Seu carrinho está pronto para receber uma boa escolha.</p><button className="primary-button" onClick={() => setCartOpen(false)}>Explorar catálogo</button></div> : <><div className="cart-items">{cart.map((item, index) => <div className="cart-item" key={`${item.id}-${index}`}><img src={item.image} alt="" /><div><strong>{item.name}</strong><span>{item.quantity} × {formatBRL(item.price)}</span>{item.hardware && <small>{item.hardware} · {item.personalization}</small>}<button onClick={() => removeFromCart(index)}>Remover</button></div></div>)}</div><div className="cart-summary"><span>Subtotal</span><strong>{formatBRL(total)}</strong></div><button className="primary-button full" onClick={() => ASAAS_CHECKOUT_URL ? window.location.assign(ASAAS_CHECKOUT_URL) : alert("Configure VITE_ASAAS_CHECKOUT_URL para ativar o checkout Asaas.")}>Ir para o checkout Asaas <ArrowRight size={17} /></button><p className="checkout-note">O checkout seguro será concluído no ambiente Asaas.</p></>}</aside></div>}</div>;
}
